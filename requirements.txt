# Core
pydantic>=2.0.0
pyyaml>=6.0

# Data sources
feedparser>=6.0.0
httpx>=0.25.0

# Browser automation (for PDF download)
playwright>=1.40.0

# PDF processing
# marker-pdf  # Install separately due to ML dependencies

# LLM APIs
openai>=1.0.0  # For DeepSeek-compatible API

# Utilities
python-dotenv>=1.0.0

# Step 4 hybrid filter (local vectors)
numpy>=1.26
sentence-transformers>=2.6.0

# Prefer CPU-only torch wheels (much smaller than default CUDA wheels).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
